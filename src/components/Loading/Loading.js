import React from 'react'
import {HashLoader} from "react-spinners";

export default function Loading() {
  //hashloader from react spinner
  return (
    
        <HashLoader
        color="#f40000"
        size={70} speedMultiplier={3}/>
   
  )
}
